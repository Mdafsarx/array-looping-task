// Use a for...of loop to concatenate all the elements of an array into a single string.

const numbers = ['Tom', 'Tim', 'Tin', 'Tik']
const join=numbers.join('');
console.log(join)
// out put=TomTimTinTik